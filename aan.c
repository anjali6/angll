#include <stdio.h>

int main(void)
{
	int n;
	scanf("%d",&n);
	if(n>0)
	{
		printf(" \n number is positive");
	}
	 else if(n<0)
	{
		printf("\n number is negative")
	}
	else
	{
		printf("\n number is zero")
	}
	
	return 0;
}
